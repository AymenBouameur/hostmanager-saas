scopes:
    
  is_active:
    label: Active ?
    type: switch
    conditions:
      - is_active = '0'
      - is_active = '1'
      
  is_pinned:
    label: Epinglé ?
    type: switch
    conditions:
      - is_pinned = '0'
      - is_pinned = '1'    
      
  created_at:
    label: Date de création
    type: daterange
    conditions: created_at >= ':after' AND created_at <= ':before'